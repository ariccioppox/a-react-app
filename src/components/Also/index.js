import './index.scss';

export default function() {
  return <div className="also">
      <p>With this app, I'm showing that I also can:</p>
      <ul>
        <li>Create a <code>React</code> app</li>
        <li>Create a <code>React</code> component</li>
        <li>Use <code>CSS</code> animations and <code>SASS</code></li>
        <li>Create an open source repository</li>
        <li>Host a <code>React</code> app on GitHub Pages</li>
      </ul>
    </div>
}