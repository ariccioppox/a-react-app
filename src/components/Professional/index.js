import './index.scss';

export default function() {
    return <div className="professional">
        <p>From my professional experiences I can:</p>
        <ul>
          <li>Create web pages from scratch</li>
          <li>Style pages with <code>HTML</code> and <code>CSS</code></li>
          <li>Code with <code>JavaScript</code> and <code>jQuery</code></li>
          <li>Connect to remote servers via <code>SSH</code></li>
          <li>Create and debug <code>bash</code> scripts</li>
          <li>Edit files with <code>nano</code> or <code>vim</code></li>
          <li>Create, clone, and fork <code>Git</code> repositories</li>
          <li>Create, merge and rebase <code>Git</code> branches</li>
          <li>Debug software solutions (including React apps)</li>
          <li>Definitely know how important it is to have readable, testable code</li>
        </ul>
      </div>
}